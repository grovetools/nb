# Documentation Task: Editor Integrations for nb

You are an expert technical writer. Based on the contents of the `nvim-plugin/` and `obsidian-plugin/` directories, create a guide for editor integrations.

**Task:**
1.  **Neovim Integration (`nb.nvim`)**:
    - Summarize the key features from `nvim-plugin/README.md` and `nvim-plugin/CHANGELOG.md`.
    - Provide the installation instructions for `lazy.nvim`.
    - List the most important default key mappings (e.g., `<leader>nn`, `<leader>ns`, `<leader>ng`, `<leader>na`).
    - Explain the purpose of the different search pickers.
2.  **Obsidian Integration**:
    - State that the plugin is experimental.
    - Briefly describe its functionality (sidebar view for browsing repositories/notes).
    - Summarize the development installation instructions from `obsidian-plugin/README.md`.