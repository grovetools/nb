name: Release
on:
  push:
    tags: ['v*']
jobs:
  release:
    uses: mattsolo1/grove-ecosystem/.github/workflows/releasable-go-tool.yml@main
    with:
      binary_name: 'nb'
    secrets:
      GITHUB_TOKEN: ${{ secrets.GROVE_PAT }}